info:
  name: Get My User Profile
  type: http
  seq: 5

http:
  method: GET
  url: "{{domain}}:{{port}}/api/{{version}}/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
